version: "3.7"
services:
  
  iam:
    image: node:16-alpine
    container_name: iam
    working_dir: /usr/src/app
    command: ["yarn", "start:dev"]
    expose:
      - 3000
    ports:
      - 3000:3000
    volumes:
      - ./:/usr/src/app:rw
